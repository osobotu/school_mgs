name: terms
columns: id, name, number

name: term_scores
columns: id, assessment, exam, subject_id(FK), term_id(FK), session_id(FK)

name: sessions
columns: id, session

name: scores 
columns: [student_id, term_scores_id](CK)


